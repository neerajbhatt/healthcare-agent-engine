version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - config/.env
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
